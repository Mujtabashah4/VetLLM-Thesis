%\newcommand{\chaptwofigs}{chap2/Figures}
\newcommand{\intso}{\int_{\textrm{SO($3$)}}}
\newcommand{\lsoo}{L^2(\textrm{SO(3)})}
\renewcommand{\bv}[1]{\boldsymbol{#1}}
\newcommand{\dfn}{\triangleq}
\newcommand{\rotop}{\mathcal{D}(\varphi,\vartheta,\omega)}
\newcommand{\rotopz}{\mathcal{D}(\varphi,\vartheta,0)}
\renewcommand{\shc}[3]{({#1})_{#2}^{#3}}
\newcommand{\lsph}{L^2(\untsph)}
\newcommand{\conj}[1]{\overline{#1}} % conjugate
\newcommand{\unit}[1]{\bv{\hat{#1}}}
\newcommand{\lsphL}[1]{\mathcal{H}_{#1}}
\newcommand{\lsphz}{\mathcal{H}^0}
\newcommand{\intsph}{\int_{\untsph}}
\newcommand{\stftf}{F}
%\newcommand{\figref}[1]{Fig.\,\ref{#1}}
\newcommand{\op}[1]{\mathcal{#1}}
\newcommand{\appref}[1]{Appendix\,\ref{#1}}
\newcommand{\secref}[1]{Section\,\ref{#1}}
\renewcommand{\diag}[1]{\textrm{diag\big($#1$\big)}}
\newcommand{\energy}{\mathcal{E}}
\newcommand{\energyinv}{\frac{1}{\energy}}
\newcommand{\rotmat}{\boldsymbol{R}}
\newcommand{\sohc}[4]{\big({#1} \big)_{#2}^{#3,#4}}
\newcommand{\diagg}[1]{\textrm{\emph{diag}\big($#1$\big)}}




\DeclarePairedDelimiterX\norm[1]{\lVert}{\rVert}{#1}
\DeclarePairedDelimiterX\abs[1]{|}{|}{#1}
\DeclarePairedDelimiterX\innerp[2]{\langle}{\rangle}{#1,#2}
\DeclarePairedDelimiterX\parn[1]{(}{)}{#1}




\chapter{Signals on the Sphere and Mathematical Background}

In this chapter, we introduce important concepts related to signals
defined on the unit sphere, in order to provide mathematical
background and to clarify the adopted notation used in later
chapters.

\section{Hilbert Space $\lsph$}

\subsection{Unit Sphere Domain}
Let $\untsph$ denote the 2-sphere or unit sphere domain, which is
defined as
\begin{align}
\untsph \dfn \{\bv{x} \in \mathbb{R}^3\colon \|\bv{x}\| = 1 \}
\end{align}
where $\bv{x}$ denotes the vector in 3-dimensional Euclidean or
Cartesian domain $\mathbb{R}^3$ and $\|\bv{x}\|$ denotes the
Euclidean norm. The points on the $\untsph$ belong to $\mathbb{R}^3$
but the unit sphere is different from $\mathbb{R}^3$ in a way that
it is bounded and has a constant non-zero curvature.

By definition, a vector representing a point on the sphere is a unit
vector, and is parameterized in terms of spherical coordinates. We
define two such unit-norm vectors, $\unit{x}$ and $\unit{y}$,
represented in the spherical coordinates as $\unit{x}\equiv
\unit{x}(\theta,\phi) \dfn (\sin\theta\cos\phi,\,
\sin\theta\sin\phi,\, \cos \theta)' \in \untsph$ and $\unit{y}\equiv
\unit{y}(\vartheta,\varphi) \dfn (\sin\vartheta\cos\varphi,\,
\sin\vartheta\sin\varphi,\, \cos \vartheta)'$ $\in \untsph$,
respectively, where $(\cdot)'$ denotes matrix or vector transpose.
$\theta, \vartheta\in [0, \pi]$ represent the co-latitude or
elevation measured with respect to the positive $z-$axis and $\phi,
\varphi\in [0, 2\pi)$ represent the longitude or azimuth and are
measured with respect to the positive $x-$axis in the $x-$$y$ plane.
The dot product between two vectors $\unit{x}$ and $\unit{y}$,
representing two points on the sphere, is related to the the central
angle $\Delta_s$ between the vectors $\unit{x}$ and $\unit{y}$,
%
\begin{align}\label{Eq:cos_delta_s_def}
\unit{x}\cdot\unit{y} = \cos \triangle_s\big((\theta,\phi ),
(\vartheta,\varphi)\big) = \sin\theta\sin\vartheta\cos(\phi-\varphi)
+ \cos\theta\cos\vartheta.
\end{align}



\subsection{Signals on the Sphere - Hilbert Space $\lsph$}

We consider complex-valued functions, such as $f(\theta,\phi) =
f(\unit{x})$ and $h(\theta,\phi) = h(\unit{x})$, defined on the unit
sphere. Define the inner product
\begin{equation}
\label{eqn:innprd}
    \innerp[\big]{f}{h} \dfn  \intsph f(\unit{x})\,\conj{h(\unit{x})} \,ds(\unit{x}),
\end{equation}
where $ds(\unit{x}) = \sin \theta\,d \theta\,d \phi$ is the area
element, $\conj{(\cdot)}$ denotes complex conjugate and the
integration is carried out over the whole unit sphere. The inner
product in \eqref{eqn:innprd} induces a norm $\|f\| \dfn\langle f,f
\rangle^{1/2}$. Finite energy functions defined on the unit sphere,
such that $\|f\| <\infty$, are referred as ``signals on the unit
sphere". Signals on the sphere form a Hilbert space
$\lsph$ under the inner product defined in \eqref{eqn:innprd}.
Throughout this thesis, functions with finite induced norm belonging
to $\lsph$ are referred as signals on the sphere or signals for
short.

\subsection{Spherical Harmonics}

The Hilbert space $\lsph$ is separable and the spherical harmonic
functions (or spherical harmonics for short) form the archetype
complete orthonormal set of basis functions. The spherical harmonic,
$Y_{\ell}^m(\unit{x}) = Y_{\ell}^m(\theta, \phi)$, for degree
${\ell} \geq 0$ and order $ |m| \leq {\ell}$ is defined
as~\cite{Sakurai:1994}
\begin{equation}
\label{Eq:Sph_harmonics}
    Y_{\ell}^m(\theta, \phi) =
    N_{\ell}^{m}P_{\ell}^{m}(\cos\theta)\,e^{im\phi},
\end{equation}
where $N_{\ell}^{m}$ is the normalization factor given by
\begin{equation}
\label{Eq:norm_constant}
    N_{\ell}^{m} \dfn \sqrt{\frac{2{\ell}+1}{4\pi}\frac{({\ell}-m)!}{({\ell}+m)!}},
\end{equation}
which ensures that $\|Y_\ell^m \| = 1$. $P_{\ell}^{m}(x)$ in
\eqref{Eq:Sph_harmonics} is the associated Legendre function defined
for degree $\ell$ and order $0 \leq m \leq \ell$
as~\cite{Sakurai:1994,kennedy:book}
\begin{align}
    P_{\ell}^{m}(x)
        &= \frac{(-1)^m}{2^{\ell} {\ell}!} (1-x^2)^{m/2} \frac{d^{{\ell}+m}}{dx^{{\ell}+m}}
            (x^2-1)^{\ell} \\
    P_{\ell}^{-m}(x)
        &= {(-1)^m} \frac{({\ell}-m)!}{({\ell}+m)!} P_{\ell}^m(x),
\end{align}
for $|x|\leq 1$. The zero order associated Legendre polynomials are
referred as Legendre polynomials ($P_{\ell}(x)$), that is,
$P_{\ell}^{0}(x) = P_{\ell}(x)$.
%
%

\subsection{Properties of Spherical Harmonics}
\subsubsection*{Orthonormality}
With the definition of spherical harmonic functions in
\eqref{Eq:Sph_harmonics}, they satisfy the orthonormality condition
%
\begin{align}
\innerp[\big]{Y_{\ell}^m}{ Y_{p}^{q}}=\delta_{\ell, p} \delta_{m,q},
\end{align}
where $\delta_{m,q}$ is the Kronecker delta function: $\delta_{m,q}
= 1$ for $m=q$ and is zero otherwise.

\subsubsection*{Spherical Harmonics Addition Theorem}
We also note one of the important property of spherical harmonics,
known as spherical harmonics addition theorem~\cite{kennedy:book}
\begin{align}\label{Eq:addition_theorem}
\sum_{m=-\ell}^\ell\, Y_\ell^m(\unit{x})\,
\overline{Y_\ell^m(\unit{y})} =
\frac{2\ell+1}{4\pi}P_\ell^0(\cos\Delta_s)
\end{align}
where $\cos\Delta_s = \unit{x}\cdot\unit{y}$ is defined in
\eqref{Eq:cos_delta_s_def}. Furthermore, the completeness relation
on the sphere is given by~\cite{kennedy:book}
\begin{align}\label{Eq:completeness_theorem}
\sum_{\ell=0}^{\infty}\sum_{m=-\ell}^\ell\, Y_\ell^m(\unit{x})\,
\overline{Y_\ell^m(\unit{y})} =(\sin\theta)^{-1}
\delta(\theta-\vartheta)\,\delta(\phi-\varphi).
\end{align}

\subsubsection*{Complex Conjugate Property}
We also note the following relation between $Y_\ell^m(\unit{x})$ and
$Y_\ell^{-m}(\unit{x})$
\begin{equation}
\label{eq:yellm:conj}
    \conj{Y_\ell^m(\unit{x})} = (-1)^mY_\ell^{-m}(\unit{x}).
\end{equation}


\subsection{Spherical Harmonic (Fourier) Transform}
By completeness of spherical harmonics~\cite{Colton:1998}, any signal $f\in \lsph$ can
be expanded as
\begin{equation}
\label{Eq:f_expansion}
    f(\unit{x})=\sum_{{\ell}=0}^{\infty}\sum_{m=-{\ell}}^{\ell} \shc{f}{\ell}{m}  Y_{\ell}^m(\unit{x}),
\end{equation}
where
\begin{equation}\label{Eq:fcoeff}
    \shc{f}{\ell}{m}\dfn\innerp[\big]{f}{Y_{{\ell}}^m} =
        \intsph f(\unit{x})\conj {Y_{\ell}^m(\unit{x})}
        \,ds(\unit{x}),
\end{equation}
is the spherical harmonic Fourier coefficient (or spherical harmonic
coefficient for short) of degree $\ell$ and order $m$. The equality
in \eqref{Eq:f_expansion} is understood in terms of convergence in
the mean~(strong convergence in the
norm)
\begin{align}
\lim_{L\rightarrow\infty} \|f(\unit{x})-
\sum_{{\ell}=0}^{L}\sum_{m=-{\ell}}^{\ell} \shc{f}{\ell}{m}
Y_{\ell}^m(\unit{x})\|=0.
\end{align}

The signal $f\in\lsph$ is said to be band-limited with maximum
spherical harmonic degree or spectral degree or band-limit $L_f$ if
$\shc{f}{\ell}{m} = 0$ for $\ell>L_f$.

The signal $f\in\lsph$ is said to be azimuthally symmetric if all of
the non-zero order spherical harmonic coefficients are zero, that
is, if $\shc{f}{\ell}{m} = 0$ for $m \neq 0$.

Using \eqref{Eq:f_expansion} and employing the orthonormality of
spherical harmonics, we can obtain the following Parseval relation
%
\begin{align}
\|f\|^2 = \innerp[\big]{f}{f} =
\sum_{{\ell}=0}^{\infty}\sum_{m=-{\ell}}^{\ell}|\shc{f}{\ell}{m}|^2.
\end{align}
%
\subsubsection{Shorthand Notations and Vector Notation} In the
following we may use the following shorthand notations
\begin{align}
\sum_{\ell=0}^\infty\sum_{m=-\ell}^\ell \triangleq
\sum_{\ell,m}^\infty, \quad \quad \sum_{\ell=0}^L\sum_{m=-\ell}^\ell
\triangleq \sum_{\ell,m}^L
\end{align}
for brevity. For notational simplifications, we may also express the
spherical harmonic $Y_{\ell}^m$ as $Y_n$ and the spherical harmonic
coefficient $ \shc{f}{\ell}{m}$ as $\shc{f}{n}{}$. That is, as a
function of a single integer index $n$ instead of two integer
indices ${\ell}$ and $m$, using the \emph{one-to-one} mapping
    \begin{equation}
    \label{Eq:mapping}
        ({\ell},m)\leftrightarrow n, \quad n = {\ell}^2+{\ell}+m, \quad \ell
            = \lfloor \sqrt{n}\rfloor, \quad m = n - \lfloor \sqrt{n}
            \rfloor(\lfloor \sqrt{n}\rfloor-1),
    \end{equation}
where $\lfloor\,\cdot\,\rfloor$ denotes the integer floor function.
We will also deal with the spherical harmonic coefficients in vector
form. Using the mapping defined in \eqref{Eq:mapping}, we define
\begin{align}
\bv{f} =
\parn[\big]{\shc{f}{0}{}, \, \shc{f}{1}{}, \,  \shc{f}{2}{}, \,
\cdots}' = \parn[\big]{\shc{f}{0}{0}, \, \shc{f}{1}{-1}, \,
\shc{f}{1}{0}, \, \shc{f}{1}{1}, \, \cdots}'
\end{align}
as the \emph{spectral response} of the signal $f$. For a band-limited
signal $f$ with band-limit $L_f$, $\bv{f} =
\parn[\big]{\shc{f}{0}{}, \, \shc{f}{1}{}, \,  \shc{f}{2}{}, \,
\cdots,\,\shc{f}{N_f}{}}'$, where $N_f = L_f^2+2L_f$. For
an azimuthally symmetric signal $f$, define $\bv{f}^0 =
\parn[\big]{\shc{f}{0}{0}, \, \shc{f}{1}{0}, \,  \shc{f}{2}{0}, \,
\cdots}'$.
%

\subsubsection{Spherical Harmonic (Fourier) Transform Operator}
Define the operator $\op{F}$, which transforms the signal
$f(\unit{x})$ into its spectral response $\bv{f}$ as
\begin{align}
    \bv{f} =\parn[\big]{\mathcal{F}f}(\unit{x}).
\end{align}
Also the inverse spherical harmonic transform $\mathcal{F}^{-1}$ is
well-defined such that $\mathcal{F}^{-1} \bv{f} = f(\unit{x})$.


\subsection{Spherical Dirac Delta Function}

The Dirac delta  function $\delta(\unit{x}, \unit{y})$ on the sphere
with the sifting property
\begin{equation}
\label{Eq:delta_def}
    f(\unit{x})= \intsph \delta(\unit{x}, \unit{y})f(\unit{y}) \,ds(\unit{y}),
\end{equation}
has following expansion in spherical harmonic domain~(see
completeness relation in \eqref{Eq:completeness_theorem})
\begin{equation}
\label{Eq:delta_def_expansion}
    \delta(\unit{x}, \unit{y}) =
        \sum_{\ell,m}^\infty Y_\ell^m(\unit{x})
        \conj{Y_\ell^m(\unit{y})}.
\end{equation}
Note that $\delta(\unit{x}, \unit{y}) \notin \lsph$.
%

\subsection{Useful Subspaces of $\lsph$}

The set of \emph{band-limited} signals, such as $f(\unit{x}) \in
\lsph$, with the maximum spectral degree $L_f$ such that
$\shc{f}{\ell}{m} = 0$ for $\ell > L_f$ forms a $(L_f+1)^2$
dimensional closeed subspace of $\lsph$ and is denoted by $\lsphL{L_f}$.

The set of azimuthally symmetric functions which are independent of
the azimuth angle (such that $f(\unit{x}) = f(\theta, \phi) =
f(\theta)$) forms a subspace of $\lsph$ and is denoted by $\lsphz$.
In this case, only the zero-order spherical harmonic coefficients of $f$
are non-zero. That is $\shc{f}{\ell}{m} = 0$ for all $m\neq 0$.
Mathematically, any signal $f\in\lsphz$ can be expressed as
%
\begin{align}
f(\theta,\phi) = f(\theta) &= \sum_{\ell=0}^\infty \shc{f}{\ell}{0}
Y_\ell^0 \nonumber \\ &=\sqrt{\frac{2\ell+1}{4\pi}} \shc{f}{\ell}{0}
P_\ell(\cos\theta).
\end{align}
Completeness of $\lsphz$ follows from the completeness of Legendre
polynomials~\cite{kennedy:book}.


\subsection{Spherical Harmonics Triple Product}
The spherical harmonics triple product is given by

\begin{equation}
\label{Eq:triple_prod_abc}
    T(u;r;n) = T(s,t;p,q;\ell,m)= \dfn \intsph Y_r(\unit{x}) Y_u(\unit{x}) \conj{Y_n(\unit{x})} \,ds(\unit{x})
\end{equation}
with mappings $(s,t)\leftrightarrow u$, $(p,q)\leftrightarrow r$ and
$(\ell,m)\leftrightarrow n$. The triple product can be expressed in
terms of Wigner-$3j$ symbols~\cite{Sakurai:1994} as
    \begin{align}
    \label{Eq:Def_triple_product}
        T(u;r;n) =T(s,t;p,q;\ell,m)&= (-1)^m \,
            \sqrt{\frac{(2s+1)(2p+1)(2\ell+1)}{4\pi}} \,
%        \renewcommand{\arraystretch}{0.65}
        \nonumber \\ & \quad \quad \quad
        \begin{pmatrix}
          s & p & \ell \\
          0 & 0 & 0
        \end{pmatrix}
        \begin{pmatrix}
          s & p & \ell \\
          t & q & -m
        \end{pmatrix}.
    \end{align}

We note that $T(u;r;n) = T(r;u;n)$. We further note that the
Wigner-$3j$ symbols~\cite{Sakurai:1994} are real-valued. Therefore,
$\conj{T(u;r;n)} = T(u;r;n)$, which can also be directly proven
using $\eqref{Eq:triple_prod_abc}$, symmetry relations of
Wigner-$3j$ symbols and the fact that $T(u;r;n)$ is non-zero only
when $s+p+\ell$ is even.

\section{Rotations on the Sphere}
Rotations on the sphere serve as counterparts of translations of the
Euclidean domain. We define the effect of rotations on the signal
defined on the sphere as an operator. We first define the rotation
operator and later we note the effect of rotation in spectral
domain.

\subsection{Rotation Operator}
Rotations on the sphere are often parameterized using Euler angles
$(\varphi,\vartheta,\omega)\in \textrm{SO(3)}$, where $\varphi\in
[0,\,2\pi)$, $\vartheta\in [0,\,\pi]$ and $\omega \in
[0,\,2\pi)$~\cite{Sakurai:1994}. Using the `$zyz$' Euler convention,
we define the rotation operator $\mathcal{D}_\rho = \rotop$, which
rotates a function on a sphere in the sequence of $\omega$ rotation
around the $z$-axis, then $\vartheta$ rotation about the $y$-axis followed
by a $\varphi$ rotation around the $z$-axis. If a function $f(\theta,
\phi)$ is rotated on the sphere, then
\begin{equation}
\big({\mathcal{D}_\rho f}\big)(\unit{x}) =  \big({\rotop
f}\big)(\unit{x}) \triangleq f(\rotmat^{-1} \unit{x}),
\end{equation}
where $\rotmat$ is the $3 \times 3$ rotation matrix corresponding to
the rotation operator $\rotop$ and is given by~\cite{kennedy:book}
%
\begin{equation}
\label{Eq:Rotation_matrix_R}
    \rotmat=
    \begin{pmatrix}
        \cos\varphi\cos\vartheta\cos\omega -\sin\varphi\sin\omega & -\cos\varphi\cos\vartheta\sin\omega-\sin\varphi\cos\omega & \cos\varphi\sin\vartheta \\
        \sin\varphi\cos\vartheta\cos\omega -\cos\varphi\sin\omega & -\sin\varphi\cos\vartheta\sin\omega+\cos\varphi\cos\omega & \sin\varphi\sin\vartheta \\
        \sin\vartheta\cos\omega  & \sin\vartheta\sin\omega &     \cos\vartheta
    \end{pmatrix}.
\end{equation}
The inverse of $\rotop$ denoted by $\rotop^{-1}$ is
$\op{D}(-\omega,-\vartheta,-\varphi)$.


\subsection{Effect of Rotation in Spectral Domain}
If a signal $f(\theta, \phi)$ is rotated on the sphere under
rotation operator $\rotop$, the spherical harmonic coefficient, of
degree $\ell$ and order $m$, of the rotated signal is a linear
combination of different order spherical harmonic coefficients of
the original signal of the \emph{same} degree $\ell$
as~\cite{Sakurai:1994}
\begin{equation}
\label{eqn:rotation_non_symm}
    \parn[\big]{\rotop f}_\ell^m \dfn \innerp[\big]{\rotop f}{Y_\ell^m} =
        \smashoperator{\sum_{m'=-{\ell}}^{{\ell}}} D^{m,m'}_{\ell}(\varphi,\vartheta,\omega)
        \shc{f}{\ell}{m'},
\end{equation}
where $D^{m,m'}_{\ell}(\varphi,\vartheta,\omega) =
D^{m,m'}_{\ell}(\rho)$ is the Wigner-$D$ function defined in next
subsection.

\subsection{Wigner-$D$ Function}

The Wigner-$D$ function is defined as
%
\begin{equation}
\label{Eq:Dlm}
 D_{\ell}^{m,m'}(\rho) =    D_{\ell}^{m,m'}(\varphi,\vartheta,\omega) =
        e^{-im\varphi}d^{m,m'}_{\ell}(\vartheta)e^{-im'\omega},
        \quad \rho = (\varphi,\vartheta,\omega) \in \textrm{SO(3)}
\end{equation}
where $|m|,\,|m'| \leq \ell$ and $d^{mm'}_{\ell}(\vartheta)$ is the
Wigner-$d$ function~\cite{Sakurai:1994} given by
%
\begin{multline}
\label{Eq:wigner_d}
    \qquad d_{\ell}^{m,m'}(\theta)
        = \sum_n (-1)^{n-m'+m} \times{}
        \frac{\sqrt{({\ell}+m')!({\ell}-m')!({\ell}+m)!({\ell}-m)!}}
            {{({\ell}+m'-n)!(n)!({\ell}-n-m)!(n-m'+m)!}} \times{} \\
        \cos \Big(\frac{\theta}{2}\Big)^{2{\ell}-2n+m'-m}
            \sin\Big(\frac{\theta}{2}\Big)^{2n-m'+m},\qquad
\end{multline}

We note the following relation between spherical harmonic function
and Wigner-$D$ function
\begin{equation}
\label{eqn:wignerD:Y}
    D^{m,0}_{\ell}(\varphi,\vartheta,0)=\sqrt{\frac{4\pi}{2\ell+1}}
        \conj{Y_\ell^m(\vartheta,\varphi)}.
\end{equation}


\subsection{Rotation of Azimuthally Symmetric Function}
For the azimuthally symmetric functions $h(\unit{x}) \in \lsphz$,
the $\omega$ rotation around $z-$axis becomes ineffective and can be
set to $\omega = 0$. Hence, the expression in
\eqref{eqn:rotation_non_symm} simplifies to
    \begin{equation}
    \label{eqn:rotation_symm}
        \parn[\big]{\rotopz h}_\ell^m = D^{m,0}_{\ell}(\varphi,\vartheta,0)
            \shc{h}{\ell}{0}=\sqrt{\frac{4\pi}{2\ell+1}}
            \conj{Y_\ell^m(\vartheta,\varphi)} \shc{h}{\ell}{0},
    \end{equation}
where the second equality follows from the relation between
spherical harmonic and Wigner-$D$ function given in
\eqref{eqn:wignerD:Y}.

\section{Signals on the Rotation Group ${\textrm{SO(3)}}$ }
For $\ell \geq0$ and $m,m' \in \mathbb{Z}$ such that
$|m|,|m'|\leq\ell$, the Wigner-$D$ functions in \eqref{Eq:Dlm} form
a complete set of orthogonal functions for the space $\lsoo$ of
functions defined on the rotation group SO($3$) and follow the
orthogonality relation
%
\begin{align}\label{Eq:WignerD_ortho}
\intso D_{\ell}^{m,m'}(\rho) \overline{D_{p}^{q,q'}(\rho)}\, d\rho =
 \frac{8 \pi^2}{2\ell +1}\,\delta_{\ell p} \delta_{mq}
\delta_{m'q'},
\end{align}
%
where $d\rho = d\varphi \sin \vartheta d\vartheta  d\omega$ and the
integral is a triple integral over all rotations
$(\varphi,\vartheta,\omega)\in \textrm{SO(3)}$~\cite{Sakurai:1994}.
Thus, any function $f\in\lsoo$ may be expressed as
%
\begin{align}\label{Eq:g_expansion_Wigner}
f(\rho)\,=\, \sum_{\ell=0}^\infty
\sum_{m=-\ell}^{\ell}\sum_{m=-\ell'}^{\ell'}\sohc{f}{\ell}{m}{m'}
D_{\ell}^{m,m'}(\rho) ,
\end{align}
where
%
\begin{align}
\sohc{f}{\ell}{m}{m'}\,=\, \frac{2\ell +1}{8 \pi^2} \intso
f(\rho)\overline{D_{\ell}^{m,m'}(\rho)}\,d\rho.
\end{align}
%
The signal $f$ is said to be band-limited with maximum degree $L_f$
if $\sohc{f}{\ell}{m}{m'}=0,\,\forall \ell>L_f$.

\section{Summary}\label{ch2:sec:conclusions}
In this chapter, we have introduced the required mathematical
background. The notation adopted in this chapter is used throughout
this thesis. However, the new notation or formulation, if required,
will be defined in later chapters.
